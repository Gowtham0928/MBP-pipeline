pipeline {
    agent any
    stages {
        stage('deploy') {
            steps {
                echo "this is goutham"
                echo "siva"
            }
        }
        stage('prod') {
            steps {
                echo "go to production"
            }
        }
    }
}
